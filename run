c++ -std=c++14 main.cpp; 
if [ $? -eq 0 ]; then 
    start_time="$(date -u +%s)"
    ./a.out;
    end_time="$(date -u +%s)"
    elapsed="$(($end_time-$start_time))"
    echo "Total of $elapsed seconds elapsed for process" 
fi;